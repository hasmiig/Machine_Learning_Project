def train_baseline(X_train, y_train):
    """Train a baseline model on the tabular features.

    Planned work: fit a linear regression as a sanity-check baseline, then
    a random forest for a stronger non-linear baseline. Returns the fitted
    model object.
    """
    pass
